# My Awesome Flutter Project

A short description of what the project does and its purpose, e.g., a simple todo list app using the BLoC state management pattern.

## Getting Started

This project is a starting point for a Flutter application.

### Prerequisites

*   **Flutter SDK**: Ensure you have the Flutter SDK installed on your system. You can check with:
    ```bash
    flutter --version
    ```
*   **Firebase (if applicable)**: If the project uses Firebase, you will need your own Firebase project and the Firebase CLI installed.

### Installation

1.  **Clone the repository**:
    ```bash
    git clone https://github.com
    cd MZRPO-UI
    ```
2.  **Install dependencies**:
    ```bash
    flutter pub get
    ```
3.  **Run code generation (if applicable, e.g., for `build_runner` or `flutterfire`)**:
    ```bash
    flutter pub run build_runner build 
    # or 
    flutterfire configure 
    ```

### Usage

To run the app in debug mode:

```bash
flutter run
